'use client';

import { PageHeader } from '@/components/ui';

export default function HomePage() {
  return <PageHeader title="Home" description="Placeholder — dashboard is built later in this pass." />;
}
